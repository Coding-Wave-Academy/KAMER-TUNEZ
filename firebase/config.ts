// This is a mock Firebase configuration file.
// In a real Firebase project, you would initialize your app here
// with the configuration object from your Firebase project settings.

/*
import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";

const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "...",
  appId: "1:..."
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);

*/

// For now, we'll just export a mock object.
export const db = {};
